#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../clientjade/bin/clientjade" "$@"
  ret=$?
else 
  node  "$basedir/../clientjade/bin/clientjade" "$@"
  ret=$?
fi
exit $ret
